# Secrets — keep `.env` on your machine only; never commit it
.env

# Virtual environment
.venv/
venv/

# LangGraph checkpoints (default path; add other DB names if you use --checkpoint-db)
workflow_checkpoints.sqlite

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
dist/
build/

# OS / editors
.DS_Store
.idea/
.vscode/
