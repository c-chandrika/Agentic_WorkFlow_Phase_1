[
  {
    "question": "Let $A$ be an $m \\times n$ matrix with rank $r$. How many non-zero singular values does $A$ have?",
    "options": [
      "$m$",
      "$n$",
      "$r$",
      "$\\min(m, n)$"
    ],
    "answer": "$r$",
    "explanation": "The number of non-zero singular values of a matrix is equal to its rank. Singular values provide a measure of the 'strength' of the linear transformation along principal directions, and their count directly corresponds to the dimensionality of the image space, which is the rank.",
    "level": "l3",
    "topic": "Linkear algebra"
  },
  {
    "question": "A complex $n \\times n$ matrix $A$ is similar to a diagonal matrix if and only if:",
    "options": [
      "$A$ is symmetric.",
      "The algebraic multiplicity of each eigenvalue equals its geometric multiplicity.",
      "$A$ is invertible.",
      "The minimal polynomial of $A$ has degree $n$."
    ],
    "answer": "The algebraic multiplicity of each eigenvalue equals its geometric multiplicity.",
    "explanation": "A matrix is diagonalizable if and only if for every eigenvalue, its algebraic multiplicity (the power of $(\\lambda - \\lambda_i)$ in the characteristic polynomial) is equal to its geometric multiplicity (the dimension of the eigenspace corresponding to $\\lambda_i$). This ensures a full set of linearly independent eigenvectors to form a basis for diagonalization.",
    "level": "l3",
    "topic": "Linkear algebra"
  },
  {
    "question": "Which of the following is a necessary and sufficient condition for a symmetric matrix $A$ to be positive definite?",
    "options": [
      "All eigenvalues of $A$ are non-negative.",
      "All principal minors of $A$ are non-negative.",
      "For every non-zero vector $x$, $x^T A x > 0$.",
      "$A$ is invertible."
    ],
    "answer": "For every non-zero vector $x$, $x^T A x > 0$.",
    "explanation": "By definition, a symmetric matrix $A$ is positive definite if $x^T A x > 0$ for all non-zero vectors $x$. This is equivalent to all eigenvalues being strictly positive, or all leading principal minors being strictly positive.",
    "level": "l3",
    "topic": "Linkear algebra"
  },
  {
    "question": "Let $V$ and $W$ be finite-dimensional vector spaces over a field $F$ with $\\dim(V) = m$ and $\\dim(W) = n$. What is the dimension of their tensor product $V \\otimes W$?",
    "options": [
      "$m+n$",
      "$mn$",
      "$\\max(m, n)$",
      "$m^n$"
    ],
    "answer": "$mn$",
    "explanation": "If $\{v_1, \\dots, v_m\}$ is a basis for $V$ and $\{w_1, \\dots, w_n\}$ is a basis for $W$, then the set of elementary tensors $\{v_i \\otimes w_j \\mid 1 \\le i \\le m, 1 \\le j \\le n\}$ forms a basis for $V \\otimes W$. The number of elements in this basis is $mn$.",
    "level": "l3",
    "topic": "Linkear algebra"
  },
  {
    "question": "Which of the following is NOT a defining property of the Moore-Penrose pseudoinverse $A^+$ for a matrix $A$?",
    "options": [
      "$A A^+ A = A$",
      "$A^+ A A^+ = A^+$",
      "$(A A^+)^T = A A^+$",
      "$A^+ = (A^T A)^{-1} A^T$ (when $A^T A$ is invertible)"
    ],
    "answer": "$A^+ = (A^T A)^{-1} A^T$ (when $A^T A$ is invertible)",
    "explanation": "The Moore-Penrose pseudoinverse $A^+$ is uniquely defined by the four Penrose conditions: 1) $A A^+ A = A$, 2) $A^+ A A^+ = A^+$, 3) $(A A^+)^T = A A^+$, and 4) $(A^+ A)^T = A^+ A$. The expression $A^+ = (A^T A)^{-1} A^T$ is a specific formula for $A^+$ when $A$ has full column rank, but it is not one of the general defining properties applicable to all matrices.",
    "level": "l3",
    "topic": "Linkear algebra"
  },
  {
    "question": "A complex $n \\times n$ matrix $A$ is unitarily diagonalizable if and only if $A$ is:",
    "options": [
      "Hermitian",
      "Symmetric",
      "Normal",
      "Unitary"
    ],
    "answer": "Normal",
    "explanation": "The Spectral Theorem for normal operators states that a complex matrix $A$ is unitarily diagonalizable (i.e., there exists a unitary matrix $U$ such that $U^*AU$ is diagonal) if and only if $A$ is normal, meaning $A A^* = A^* A$. Hermitian and Unitary matrices are specific types of normal matrices.",
    "level": "l3",
    "topic": "Linkear algebra"
  },
  {
    "question": "Let $V$ be a finite-dimensional vector space and $W$ be a subspace of $V$. What is the dimension of the quotient space $V/W$?",
    "options": [
      "$\\dim(V) + \\dim(W)$",
      "$\\dim(V) - \\dim(W)$",
      "$\\min(\\dim(V), \\dim(W))$",
      "$\\dim(V) \\cdot \\dim(W)$"
    ],
    "answer": "$\\dim(V) - \\dim(W)$",
    "explanation": "The dimension of the quotient space $V/W$ is given by the formula $\\dim(V/W) = \\dim(V) - \\dim(W)$. This means that the quotient space effectively 'factors out' the dimensions contributed by the subspace $W$.",
    "level": "l3",
    "topic": "Linkear algebra"
  },
  {
    "question": "For a normal matrix $A \\in \\mathbb{C}^{n \\times n}$, which of the following statements about its spectral radius $\\rho(A)$ and its 2-norm (spectral norm) $||A||_2$ is true?",
    "options": [
      "$\\rho(A) = ||A||_2$",
      "$\\rho(A) \\le ||A||_2$",
      "$\\rho(A) \\ge ||A||_2$",
      "$\\rho(A)$ is always strictly less than $||A||_2$ unless $A$ is the zero matrix."
    ],
    "answer": "$\\rho(A) = ||A||_2$",
    "explanation": "For any matrix, the spectral radius is always less than or equal to any induced matrix norm ($\\rho(A) \\le ||A||$). However, for a normal matrix, the spectral radius is specifically equal to its 2-norm (spectral norm), which is defined as the largest singular value of $A$.",
    "level": "l3",
    "topic": "Linkear algebra"
  },
  {
    "question": "According to the Perron-Frobenius theorem, for a positive matrix $A$ (i.e., all entries $a_{ij} > 0$), which of the following is true about its dominant eigenvalue $\\lambda_1$?",
    "options": [
      "$\\lambda_1$ is real and positive, and its corresponding eigenvector can be chosen to have all positive components.",
      "$\\lambda_1$ is always 1.",
      "$\\lambda_1$ can be complex.",
      "$\\lambda_1$ has a geometric multiplicity greater than 1."
    ],
    "answer": "$\\lambda_1$ is real and positive, and its corresponding eigenvector can be chosen to have all positive components.",
    "explanation": "The Perron-Frobenius theorem states that a positive matrix $A$ has a unique largest eigenvalue $\\lambda_1$ that is real and positive. This eigenvalue is simple (its algebraic and geometric multiplicities are 1), and its corresponding eigenvector can be chosen such that all its components are strictly positive.",
    "level": "l3",
    "topic": "Linkear algebra"
  },
  {
    "question": "Let $P$ be an $n \\times n$ matrix. $P$ is an orthogonal projection matrix if and only if:",
    "options": [
      "$P^2 = P$ and $P = P^T$",
      "$P^2 = I$ and $P = P^T$",
      "$P^2 = P$ and $P$ is invertible",
      "$P P^T = I$"
    ],
    "answer": "$P^2 = P$ and $P = P^T$",
    "explanation": "An orthogonal projection matrix $P$ must satisfy two conditions: it is idempotent ($P^2 = P$) meaning applying the projection twice is the same as applying it once, and it is symmetric ($P = P^T$) meaning it projects onto a subspace orthogonally. The other options describe reflections ($P^2=I, P=P^T$), the identity matrix ($P^2=P, P$ invertible implies $P=I$), or orthogonal matrices ($PP^T=I$).",
    "level": "l3",
    "topic": "Linkear algebra"
  }
]